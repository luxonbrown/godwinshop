{
  "name": "godwinshop",
  "version": "1.0.0",
  "private": true,
  "description": "GodwinShop — full-stack commercial e-commerce platform (React + Express + MySQL)",
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "setup": "npm --prefix server run setup",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
